package helper

// InsertLocation inserts a new location into PostgreSQL
func InsertLocation(userID string, lat, lon float64) {
	// _, err := database.DB.Exec("INSERT INTO locations (user_id, latitude, longitude) VALUES ($1, $2, $3)", userID, lat, lon)
	// if err != nil {
	// 	log.Println("PostgreSQL Insert Error:", err)
	// }
}
