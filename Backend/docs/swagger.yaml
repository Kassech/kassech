basePath: api
host: localhost:5000/
info:
  contact:
    name: Team Kassech
  description: A comprehensive API for managing cars, offering endpoints for creation,
    update, deletion, and retrieval of transport data.
  title: Kassech Transport Management API
  version: "1.0"
paths: {}
schemes:
- http
- https
securityDefinitions:
  JWT:
    description: JWT security accessToken. Please add it in the format "Bearer {AccessToken}"
      to authorize your requests.
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
